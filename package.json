{
  "name": "httproxy",
  "description": "A HTTP proxy middleware for merge request",
  "version": "0.3.1",
  "engines": {
    "node": ">=6.12.3"
  },
  "main": "lib/index.js",
  "repository": "viclm/httproxy",
  "license": "MIT",
  "scripts": {
    "test": "ava test --verbose",
    "coveralls": "nyc yarn test && nyc report --reporter=text-lcov | coveralls"
  },
  "ava": {
    "babel": {
      "presets": [
        [
          "env",
          {
            "targets": {
              "node": "6.12.3"
            }
          }
        ]
      ]
    }
  },
  "dependencies": {
    "debug": "^3.1.0",
    "express": "^4.16.2",
    "form-data": "^2.3.2",
    "multer": "^1.3.0"
  },
  "devDependencies": {
    "ava": "^0.25.0",
    "babel-preset-env": "^1.6.1",
    "coveralls": "^3.0.0",
    "nyc": "^11.6.0",
    "pify": "^3.0.0",
    "sinon": "^4.4.9"
  },
  "keywords": [
    "http",
    "proxy",
    "middleware"
  ]
}
